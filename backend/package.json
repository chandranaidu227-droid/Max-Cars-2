{
  "name": "max-cars-backend",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --test tests/*.test.js",
    "test:live": "node scripts/check-supabase.js",
    "check": "node --check server.js && node --check scripts/seed-vehicles.js && node --test tests/*.test.js",
    "seed:vehicles": "node scripts/seed-vehicles.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
