# Archived MongoDB implementation

These files preserve the superseded implementation from before the Supabase migration.
They are not imported by the application or included in the active test command.
Do not run them: paths, credentials, and dependencies are no longer configured.
